---
# Set portage dependent variables
# files, directories and any names
postgres_home_dir: "{{ postgres_portage_home_dir }}"
postgres_conf_dir: "{{ postgres_portage_conf_dir }}"
postgres_data_dir: "{{ postgres_portage_data_dir }}"
postgres_bin_dir: "{{ postgres_portage_bin_dir }}"
postgres_service_name: "{{ postgres_portage_service_name }}"
