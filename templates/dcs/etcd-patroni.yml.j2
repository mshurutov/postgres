# etcdv3 settings
etcd3:
  hosts: {% for eh in groups[postgres_dcs_groups] %}{{ hostvars[eh]['common_full_hostname'] }}:{{ etcd_client_port }}{% if not loop.last %},{% endif %}{% endfor %} 
{#
  # parameter hosts is minimal required, other parameters will be defined later
  # use_proxies
  # url
  # proxy
  # srv
  # srv_suffix
  # protocol
  # username
  # password
  # cacert
  # cert
  # key
#}

